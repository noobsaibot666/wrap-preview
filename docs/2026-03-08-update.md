# 2026-03-08 Update

## Implemented Today

- Expanded raw proxy support beyond BRAW:
  - `BRAW` via `braw-decode`
  - `N-RAW` and `R3D` via `REDline`
- Fixed REDline proxy generation issues:
  - updated CLI arguments for the installed REDline build
  - handled REDline's `.mov.mov` output behavior correctly
- Improved Camera Match Lab calibration:
  - stronger chart detection for difficult frames
  - manual chart corner fitting
  - saved fit reuse for Camera B and C
- Refined match logic and UX:
  - Hero camera can be chosen explicitly
  - hero remains the anchor while other cameras match to it
  - clearer signal-only / chart-match messaging
- Reduced unnecessary app activity:
  - lighter idle polling
  - removed duplicate transform reruns in Match Lab
- Reworked Match Lab interface:
  - cleaner card spacing and alignment
  - simplified signal read layout
  - removed redundant details
  - improved action copy and hierarchy
- Reworked Production onboarding:
  - Projects are now managed directly in the onboarding card
  - recent projects can be opened or deleted inline
  - project creation moved into a modal
  - top layout now uses 3 cards on the first row and 2 on the second
- Updated global shell polish:
  - phase-colored top navigation
  - removed `Send Feedback` and `Reset App Data` from the three-dot menu

## Result

The app now has a more reliable raw ingestion path, a stronger calibration workflow, clearer production onboarding, and a more focused UI for production matching tasks.
